### JSON格式数据

+ 比起`XML`，`JSON`的主要优势在于`它的体积更小`，`在网络上传输可以省流量`，但缺点在于，它的`语义较差`，看起来不如XML直观；JSON格式的内容：

```
//中括号表示数据
[{"id":"s","version":"5.5","name":"Angry Birds"} , {"id":"6","version":"7.0","name":"Clash of Clans"} , {"id":"7","version":"3.5","name":"Hey Day"}]
```
